import styled from 'styled-components';

interface IProps {
  
}

export const StyledAppWrapper = styled.div<IProps>`
    width: 100%;
    max-width: 1360px;
    margin: auto;
    overflow: hidden;
    // padding: 0 32px;
  }
`;